import React from 'react';
import { Flex, Text } from "@chakra-ui/react";
import Header from './Header';

const Main = () => {
    return (
        <Flex>
            <Header />
        </Flex>
    );
};

export default Main;